# requirements-dashboard.txt
# Dashboard-only dependencies for Streamlit Community Cloud deployment.
# Install with: pip install -r requirements-dashboard.txt
# Or with uv:   uv pip install -r requirements-dashboard.txt

streamlit>=1.40.0
duckdb>=1.1.0
plotly>=5.22.0
pandas>=2.1.0
pyarrow>=16.0.0
numpy>=1.26.0
